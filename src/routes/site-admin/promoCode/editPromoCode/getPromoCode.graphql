query($id: Int!) {
    getPromoCode(id: $id) {
        result{
        id
        title
        description
        imageName
        code
        type
        promoValue
        currency
        expiryDate
        isEnable
        }
    }
}